<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.claimit">

    <!--  Permission to access internet for API integration  -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--  Permission to access to the External storage  -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ClaimIt">
        <activity
            android:name=".SignInActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.ClaimIt.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:theme="@style/Theme.ClaimIt.NoActionBar">
        </activity>
        <activity android:name=".ImageUploadActivity">
        </activity>
        <activity android:name=".ClaimFormActivity">
        </activity>
    </application>

</manifest>